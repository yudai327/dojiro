version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    container_name: dojiro-app
    volumes:
      - .:/workspace:cached
    working_dir: /workspace
    ports:
      - "3000:3000"
    env_file:
      - .env.local
