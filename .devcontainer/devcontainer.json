{
  "name": "Dodgeball Score Manager",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "app",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "forwardPorts": [
    3000
  ],
  "remoteUser": "vscode",
  "postCreateCommand": "bash -lc 'if [ -f package.json ]; then npm install; else for d in frontend app web client ui src; do if [ -f \"$d/package.json\" ]; then (cd \"$d\" && npm install); exit 0; fi; done; echo \"No package.json found in workspace root or common subdirs; skipping npm install\"; fi'"
}